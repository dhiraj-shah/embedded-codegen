// Auto-generated GPIO init
// Generated on {{ now.strftime("%Y-%m-%d %H:%M:%S") }}

#include "config.h"
#ifdef ENABLE_GPIO
#include "gpio.h"
#include "hal.h"

void gpio_init(void) {
{% for gpio in board.gpio %}
    configure_pin("{{ gpio.pin }}", "{{ gpio.mode }}", "{{ gpio.pull }}", "{{ gpio.speed }}");
{% endfor %}
}
#endif

